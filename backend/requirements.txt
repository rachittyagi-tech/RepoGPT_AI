# =========================================================
# RepoGPT AI — Backend Requirements (Step 2: Core Foundation)
# Pinned to mutually compatible versions (tested combination).
# GitHub / RAG / ChromaDB / Gemini deps land in a later step.
# =========================================================

# ---------------- Core Framework ----------------
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
starlette==0.37.2

# ---------------- Settings / Validation ----------------
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ---------------- ASGI / HTTP utils ----------------
python-multipart==0.0.9
httpx==0.27.0

# ---------------- Authentication & User Management (Step 11) ----------------
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
email-validator==2.2.0

# ---------------- GitHub Repository Module (Step 3) ----------------
GitPython==3.1.43

# ---------------- Code Processing Pipeline (Step 5) ----------------
# Using the lightweight langchain-core + langchain-text-splitters packages
# instead of the full "langchain" meta-package — we only need Document
# and the RecursiveCharacterTextSplitter/Language-aware splitters here.
langchain-core==0.2.9
langchain-text-splitters==0.2.1

# ---------------- Embedding Generation Layer (Step 6) ----------------
# Gemini provider:
google-generativeai==0.5.4
# HuggingFace provider (local, no API key needed):
sentence-transformers==3.0.1

# ---------------- ChromaDB Vector Store Layer (Step 7) ----------------
chromadb==0.5.0

# ---------------- RAG Pipeline (Step 8) ----------------
tiktoken==0.7.0

# ---------------- Testing ----------------
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# ---------------- Code Quality ----------------
ruff==0.4.9
black==24.4.2
mypy==1.10.0
